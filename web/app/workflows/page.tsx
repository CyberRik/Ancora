import { Placeholder } from "@/components/placeholder";

export default function WorkflowsPage() {
  return (
    <Placeholder title="Workflows" phase="Phase 1–3">
      Run list, interactive DAG view, and the node inspector land as the durable
      engine and node library come online.
    </Placeholder>
  );
}
