import { Placeholder } from "@/components/placeholder";

export default function WorkersPage() {
  return (
    <Placeholder title="Workers" phase="Phase 2">
      Worker pools, GPU/CPU/memory utilization, and registered capabilities appear
      once the execution runtime and Ray bridge are wired.
    </Placeholder>
  );
}
