# Base URL of the Ancora API Gateway, used by the dashboard (browser-side).
NEXT_PUBLIC_API_URL=http://localhost:8080

# Enable Demo Mode to use mock seed data and bypass the backend
NEXT_PUBLIC_DEMO_MODE=true
